import styled from "styled-components/macro";

const Footer = () => {
  return <FooterWrapper>made with ♥️ by bwnstk</FooterWrapper>;
};

const FooterWrapper = styled.footer`
  padding: 1rem;
  text-align: center;
  height: 70px;
`;

export default Footer;
